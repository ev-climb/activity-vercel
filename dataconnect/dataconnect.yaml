specVersion: "v1"
serviceId: "activity"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "activity-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
